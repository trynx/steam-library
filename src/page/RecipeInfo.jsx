import { Space } from "antd";
import dummyRecipe from "../../dummy/recipe.json";

export const RecipeInfo = ({ recipe }) => {
  recipe = dummyRecipe;

  console.log(dummyRecipe);
  return <Space>{/* Photo */}</Space>;
};
